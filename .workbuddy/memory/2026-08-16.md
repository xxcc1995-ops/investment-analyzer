# 2026-08-16

## 完成
- 用户补充了盈利估值表完整制作口径（以万得全A为例：周度收盘/EPS/PE-TTM + 中美10Y国债 → 利差×20、估值中枢偏移率、合理收盘价、风险溢价；EPS周期=13周平滑+4% zigzag），要求研究四个指数（万得全A/沪深300/标普500/纳指100）的自动取数源。
- 五轮沙箱实测（脚本留存 `backend/scripts/test_index_data_sources*.py`，全程 `env -u HTTPS_PROXY -u HTTP_PROXY` 直连）：
  - ✅ 国债：`ak.bond_zh_us_rate`（英为财情，与用户2000-2007段同源，CN10Y+US10Y日频）；`ak.bond_china_yield`（中债官方，校验用）。
  - ✅ 沪深300：`ak.stock_index_pe_lg`（乐咕，2005-04起日频5189行，含收盘价+滚动PE；收盘价与Wind完全一致4588.2，滚动PE差~5%）；`stock_zh_index_value_csindex`（中证官网仅近20条，PE1=14.73 更贴近Wind）。
  - ⚠️ 标普500：multpl 月度表可取但 reported 口径 vs Wind operating 口径差~15%；S&P官方EPS表403。
  - ⚠️ 纳指100：stockanalysis QQQ PE=33.62 仅当前值，无免费长历史。
  - ❌ 万得全A：881001 免费源全军覆没（新浪/腾讯无、乐咕"全A"为等权中位数口径、韭圈儿API 405、东财push2沙箱被RST）。
  - ⏸ Tushare index_dailybasic 可作沪深300更优源，但 .env 的 TUSHARE_TOKEN 为空且需2000积分。
- 结论与口径差异表已写入 CLAUDE.md「指数盈利估值自动取数源」小节。

## 决策
- 待用户拍板自动化范围：沪深300+国债可立即全自动；标普500需接受口径降级；纳指100新建自攒历史；万得全A维持Wind手工或理杏仁付费API。

## 待续
- 等用户回复后实施自动更新管线（拉数→按用户公式算衍生字段→与Excel并列或追加）。

---

## 完成（第二轮：沪深300自动化管线 + 理杏仁调研）
- 用户拍板：先做沪深300+国债自动化 + 研究理杏仁API。
- **自动化管线落地**：`backend/app/services/index_earnings_auto_service.py`（乐咕收盘+滚动PE日频→ISO周重采样(周日标签=周最后交易日)；bond_zh_us_rate国债周对齐；复刻用户全套公式：隐含EPS、4周平滑+4%zigzag(自实现 `_zigzag_phases` 含首段回填)、利差×20、偏移率、合理价、风险溢价；日频落盘 `backend/data/manual/hs300_auto_cache.json` 增量更新 TTL12h）。API 复用 `/api/index-earnings`（list 追加 hs300_auto，data/{code} 分流），payload 内嵌 compare 块（Wind vs 乐咕序列+统计+中证官网校验）。前端 IndexEarnings.tsx 加「PE口径对比」图（compare 存在时渲染）。
- **实测**：1089周(2005-04-10~2026-08-16)；收盘与Wind完全一致；乐咕PE系统性低4.24%(1087周重叠)/最新-4.87%；中证官网PE1 vs 乐咕差7.79%(20日)；zigzag周期24段(手工表29段，口径差所致)；首行2005年国债4.09%合理。8299端口 API 实测全通；tsc 新增零错误。
- **理杏仁调研结论**（子代理+官方文档）：指数基本面API按市场分 cn/hk/us/index/fundamental（pe_ttm/pb/ps_ttm/dyr，日频，单次≤10年）；**不覆盖万得全A**（仅中证csi/国证cni/自研lxri源）——万得全A付费也买不到，替代方案=中证全指000985；纳指100覆盖不确定（美股接口存在但清单未列全，需注册免费试用实测）；网站会员¥518/年，API按组单独购买，新用户每接口有免费试用次数。
- CLAUDE.md 已更新（数据源表 + 模块11自动重建版说明）。

## 决策
- 万得全A自动化彻底放弃免费/理杏仁通道（均不存在881001），维持 Wind 手工导出；若未来要全市场口径自动版，改用中证全指000985新建一条自动线。
- 自动版数据不作为操作依据（口径差~4-5%），定位为手工表的"免维护对照视图"。

## 待续
- 标普500半自动（multpl口径差~15%）与纳指100新建（自攒历史）未动工，等用户确认口径取舍。

---

## 完成（第三轮 20:30：排查"功能打不开"）
- 用户反馈页面打不开。诊断：5180 前端正常；8022 进程(PID 2184) TCP 在听但请求无响应（挂死/启动中），启动日志停留在 2026-07-08。排查中进程自行恢复（推测用户刚跑过 start.bat，lifespan 启动耗时期间请求挂起）。
- 恢复后实测：8022 已含新路由 `/api/index-earnings/{list,data}`，list 返回 4 个指数（3手工+沪深300·自动），hs300_auto 0.29s 200、sp500 0.34s 200（宿主网络拉数正常）。页面已通过预览打开确认。
- 经验：start.bat 重启后 lifespan 启动期请求会长时间挂起，用户此时打开页面会看到"网络连接失败"——属短暂现象，稍等刷新即可。
