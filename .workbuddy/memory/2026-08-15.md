# 2026-08-15

## 完成
- 基金套利模块改造（需求来源：用户"套利信号.png"参考图，痛点=不知道筛选出的LOF能否申购、成交额多少）：
  - 后端 `fund_service.py`：新增 `get_em_purchase_status_map()`（akshare `fund_purchase_em` 天天基金申购状态兜底，2.7万只，含申购状态/赎回状态/日累计限定金额，1h缓存）；修东财备用源 f6 成交额被丢弃的 bug；akshare 备用源补 成交额→turnover(万元)；`_normalize_fund` 透传 est_nav。
  - 后端 `fund_arb.py`：scan/get_fund_detail 改为经 `_normalize_fund` 标准化（此前直接用原始 cell，turnover 缺失时错误回退到 amount=份额万份，"成交额"实为份额！）；`_merge_fund_data`/筛选用 turnover 不再回退 amount；est-list 申购状态 map 改按裸代码索引（原 SH/SZ 前缀查裸代码表导致全空）+ 多标的基金补申购状态 + A股LOF补 turnover + em 兜底 setdefault。
  - 后端 `fund_est.py`：`_process_single_fund` 补 turnover（新浪 fields[9] 成交额元→万元）。
  - 前端 `FundArbitragePage.tsx`：新增 `getSubscribeInfo`（✅可申(无限额)/⚠️限购(日限额X)/⛔暂停申购/❔状态未知/📦ETF不适用 徽章+提示）与 `formatTurnover`（万/亿格式化，<1000万流动性警告）；套利扫描卡片加"💡提示行"；净值估算Tab/A股LOF表名称下加徽章+成交小字。
- 验证（8023 实测）：scan turnover 真实（标普油气ETF 10.4亿、小LOF 1-200万）；est-list 申购状态生效（纳指100LOF/标普500LOF=暂停申购、美国消费LOF=限1千、海外科技LOF=限20、富国天惠=日限额2万）。前端 tsc 无本页错误。

## 决策
- 申购状态口径：集思录(主，含 min_amt 限额原文) → 天天基金 fund_purchase_em(兜底，公告级，与场内申购一致)；日累计限定金额≥1亿视为无限额。未知时前端明确标"❔状态未知"不编造（CLAUDE.md 宁可空着）。
- **集思录匿名访问各列表仅返回前20条**（rp 参数无效，total=20）——QDII LOF 多数需登录才出现在列表，故必须保留 em 兜底。

## 待续
- 宿主 8022 为旧代码，需用户管理员运行 start.bat 重启加载；8023 留有沙箱新代码实例可直接 curl 验证。

---

## 完成（第二轮：指数盈利与估值查阅模块）
- 拆解用户三个手工 Excel（标普500_1957-2026 / 万得全A1999~2026 / 沪深300_2005-2026 盈利与估值分析表）：Sheet1=说明行+表头行+周度数据+表尾（EPS周期表/◆统计摘要），「曲线图」sheet 多为嵌入图表无单元格数据（万得全A的EPS周期表在此 sheet）。口径：估值中枢偏移率=PE×十年期国债（基准线 70/60/50 对应 7/6/5 折）；合理收盘价=(100÷国债)×折扣×隐含EPS；风险溢价=100÷PE−国债；EPS周期=隐含EPS平滑+4%zigzag 红涨绿跌。
- 新增模块：后端 `index_earnings_service.py`（Excel 直读、字段名换行符归一化映射、mtime 缓存失效、周期表双 sheet 兜底）+ `api/index_earnings.py`（`/api/index-earnings/list`、`/data/{code}`，code=sp500/wind_all_a/hs300），已注册进 main.py；前端 `pages/IndexEarnings.tsx`（TabBar 选指数 + 6 StatCard + 4 张 ECharts：收盘价vs合理价(对数轴开关)/EPS红绿周期/PE+偏移率基准线/风险溢价+国债 + EPS周期区间表 + 周度数据表 + 口径说明）+ `services/api/indexEarnings.ts`，路由 `/index-earnings`，菜单「行情总览→指数盈利估值」。
- 验证：8299 端口实测三指数 list/data 全通（3623/1388/1127 行，周期 86/14/29 段）；tsc 新增文件零错误；CLAUDE.md 已补第 11 项模块文档。拆解脚本留存 `backend/scripts/analyze_index_excels.py`。

## 决策
- 数据不做副本、直读用户 Excel 原路径（mtime 缓存）——用户更新 Excel 后刷新页面即生效，单一数据源避免两份数据漂移。
- sheetagent 委派通道在本会话实测不可用（子代理无 MCP 工具），按路由规则降级 python openpyxl 直读，已向用户说明。
- 8023/8299 测试端口均被占/已清理；新路由仍需用户 start.bat 重启 8022 生效（同"待续"）。
