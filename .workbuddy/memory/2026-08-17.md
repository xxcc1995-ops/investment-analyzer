# 2026-08-17

## 完成（正式版一键启动改造 + 项目清理）
- **架构改为单进程单端口**：`main.py` 尾部新增 frontend/dist 静态托管（`StaticFiles(html=True)` mount 在 `/` + 404 处理器做 SPA 回退，/api 的 404 仍返回 JSON）。路径坑：dirname×3 即到项目根，不要再加 ".."。dist 不存在时回退为纯 API 模式。
- **静默启动器**：`启动.vbs`（双击无窗口）→ `scripts/Start-App.ps1`（强杀8022旧进程+等10秒释放+杀不掉自提权重试一次；src 比 dist 新则自动 vite build；隐藏起 uvicorn；/health 等待180s；托盘气泡通知；自动开浏览器；全程写 logs/start-app.log；exit 0）。`停止.vbs`→`scripts/Stop-App.ps1`。`start.bat` 改为兼容入口（转调 vbs）。
- **编码坑（关键教训）**：PS 5.1 解析含中文的 .ps1 必须是 **UTF-8 带 BOM**（utf-8-sig），否则报"意外的标记}"；.vbs/.bat 必须**纯 ASCII**（WSH/cmd 不认 UTF-8）。Edit/Write 工具默认写无 BOM UTF-8，写 ps1 后需用 python 转 utf-8-sig。
- **构建坑**：WorkBuddy 沙箱的 safe-delete shim 会拦截 node rmSync（vite emptyDir 清 dist 必失败 ETIMEDOUT）→ 须先用 PowerShell 原生 Remove-Item 预删 dist 再 build；宿主双击场景无此问题。顺手修了 TTradingRealtime.tsx 重复 size 属性（堵死生产的 JSX 错误）。
- **清理**：根目录冗余移入 `_archive/`（AutoIt安装包、killmarker×2、ps1_transcript、test_iv×4、restart*.bat、start_backend.bat、旧start.vbs/stop.bat、旧start.ps1、两个 stale 日志）。日志统一 `logs/`。
- **实测**：vite build 1m19s 成功；启动器两轮实测：杀旧进程→起新进程→健康通过→浏览器自动打开并 200 加载全部 SPA 资源；/index-earnings 深链接 200 text/html；API 200；/api/xyz 返回 JSON 404。
- CLAUDE.md 启动命令/访问地址章节已重写（正式模式 8022 单端口 / 开发模式手动双进程）。

## 决策
- 正式模式与开发模式并存：日常用 启动.vbs（8022 单端口）；需要 HMR 时手动起 vite 5180 + uvicorn 8022（文档在 CLAUDE.md）。
- 桌面快捷方式未建成（沙箱禁 COM），已告知用户手动右键 启动.vbs 创建。

## 待续
- 老 5180 vite dev 进程若在跑可手动关（正式模式不再需要）。
- 标普500半自动/纳指100新建仍等用户确认。
