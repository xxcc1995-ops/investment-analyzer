## 2026-07-16

### 完成 - 批次B扫尾（剩余 except-pass 分类治理）
- 背景：批次B 已根治 15 处裸 except + 交易服务(grid/t_trading)8 处 pass。本轮处理其余 ~36 处 `except Exception: pass`。
- 方法：逐站点读 try 块上下文**分类**，不盲目全转（避免给"预期回退"加日志制造噪音、淹没真告警）。
- 分类结果：
  - **函数级/掩盖故障（转 warning，5 处）**：macro.py:223(收益率曲线块)、macro.py:261(FRED批量)、futu_option_service.py:254(港股K线)、portfolio_service.py:410(组合盈亏)、right_side_service.py:3255(自选股加载)。
  - **已有后置日志（跳过，1 处）**：fund_utils.py:333 的 pass 后本就有 logger.warning。
  - **内层预期回退（保留 pass，~30 处）**：解析可选字段/逐项重试/日期解析/断开连接清理等，如 valuation:608、data_service:588、cross_analysis:1116/1125、akshare:644/1456/1540、daily_info:252/264/277/290、cb_service:719、t_position:690/719/963、tdx_source:64/94、data_provider:64、airdrop:608、overseas_news:127、fund_est_engine:118、fund_holdings:1216、macro:210/301/1062、valuation:1178/1191、right_side:115。这些是正确的"失败则跳过/用默认"，补日志属反模式。
- 配套：给 macro.py、futu_option_service.py 补了 `import logging`+`logger`（原无日志机制）。
- 验证：4 个改动文件 py_compile OK；`import app.main`=IMPORT OK。
- 决策依据：真金白银工具，告警要"少而准"——给 30 个预期回退加 debug/warning 会让真告警被淹没；只对"整段吞异常掩盖上游故障"的 5 处补 warning，才是有效可观测性。

### 完成 - 回测疑点根因定位（确认 bug）
- 疑点：CLAUDE.md 表中三策略（安道全面值/双低/摊大饼）年化15.54%/回撤-13.43%/夏普0.93 完全相同。
- 根因（`services/cb_backtest_service.py`）：
  1. **`sell_rule` 是死配置**：STRATEGIES 里定义 6 次（line 54/62/70/81/92/103），但全文件再无引用。卖出逻辑(line 653-680)只卖"轮出 top_n 的持仓"，不看 sell_rule(price>=130/140/150)。grep `sell_rule` 仅 6 处全在定义处。
  2. **filter 截断失效**：三策略 filter 为 price≤115/≤130/≤140，sort_key 全是 price 升序、top_n=15。当 universe 始终有 ≥15 只 price≤115 的转债时，三策略选出的"最便宜 15 只"完全相同（115⊂130⊂140，最便宜15只总在115内）。
  3. 唯一差异(filter 截断)在≥15只低价券存在时失效 + sell_rule 死代码 → 三策略在模拟中**计算上等价** → 指标必然相同。非巧合，是逻辑必然。
- 影响：CLAUDE.md「可转债策略回测验证(2024)」表声称验证了3个不同策略，实际三者在该实现下完全等价，结论不可信。真金白银决策若依赖该表会被误导。
- 修复方向（未实施，需用户确认）：a) 让 sell_rule 真正生效（解析价格阈值、盘中触发卖出，非仅轮出）；b) 让 filter 在低价券不足时产生差异化选股；c) 重跑三策略得真实差异化结果并更新 CLAUDE.md 表。
- 决策：本次仅定位根因，未改代码（改回测引擎逻辑属设计变更、风险高，且需重跑验证）。

### 完成 - 回测 bug 修复实施（sell_rule 生效）
- 用户确认"实施"。改动 `services/cb_backtest_service.py`：
  1. STRATEGIES 6 个策略各新增 `sell_fn`（lambda p->bool），保留 `sell_rule` 字符串（API/前端展示用，cb_backtest.py:25 仅取 key/name/description/sell_rule，sell_fn 不泄漏）：
     - andaoquan sell_fn: p>=130；dual_low: p>=140；pancake: p>=150；ytm_defense: p>=125；revision_game: p>=135 or p<=90；redeem_game: p>=140 or p<=105。（"not in top_n"/"rating_downgrade" 仍由调仓轮出/无法回测，注释说明）
  2. `_simulate_strategy`：提取 `sell_fn`；在每日循环里、调仓前，新增"盘中止损/止盈"卖出块——遍历持仓，sell_fn(当日价)为真则卖出（reason='止盈/止损'），卖后重算 total_value 供调仓买入用。
- 验证（合成数据单元测试，B01 路径 100→145 跨越 130/140）：
  - andaoquan B01卖出@130(止盈/止损)，末值101534.72
  - dual_low B01卖出@140(止盈/止损)，末值102534.52
  - pancake B01卖出@145(轮出)，末值103042.32
  - 断言三策略卖出价不同 → **PASS**。修复前三者完全相同。
- CLAUDE.md：回测表加"数据已失效待重跑"警示，说明 bug 与修复，要求重跑 2024 真实数据后再用、此前不得据此表做实盘决策。
- 验证：py_compile OK；import OK；合成测试 PASS。临时测试文件已删。
- 未做：真实数据重跑（需网络拉 2024 全年转债历史，慢；且结果应三策略不同，需用户在宿主环境跑 `/api/cb-backtest/run` 三次确认后更新 CLAUDE.md 表）。

### 完成 - 批次C 行情旁路统一（调查+DRY重构）
- 调查结论：报告所说"20处旁路"**多数不适用 multi_source**：
  - 指数点位/汇率(daily_info 新浪指数、data_service HKDCNY、fund_service USDCNY)：multi_source 不覆盖指数/汇率。
  - K线/列表(eastmoney clist/kline、fund_holdings、fund_t_strategy、real_backtest)：multi_source 不提供历史/列表。
  - data_service:131/309 是 `_get_stock_basic_fallback`/`_get_hk_stock_basic`——multi_source 失败后的**正确回退**，非旁路。
  - 真正纯个股现价/名称旁路仅 ~5 处(cigar_butt/national_team/right_side/vi/relative_valuation)。
- 执行的 DRY 重构（取名称重复消除）：
  - core/utils.py 新增 `fetch_tencent_names(symbols, timeout)` → {symbol:name}，统一腾讯 qt.gtimg.cn 批量取名称（GBK+headers+解析），异常返回 {}。
  - right_side_service `_get_stock_name`：去掉 try/requests/split，改调 fetch_tencent_names([sym]).get(sym, stock_code)，行为保持(失败回退代码)。
  - relative_valuation_service `_fetch_universe_names`：去掉 _requests+内联解析，改调 fetch_tencent_names(syms) + zip 映射回 code，行为保持。
- 验证：py_compile OK；import app.main OK；**联网实测** fetch_tencent_names(['sh600519','r_hk00700','sz000001']) → {'sh600519':'贵州茅台','r_hk00700':'腾讯控股','sz000001':'平安银行'} PASS（A股+港股+批量解析全对）。
- 未做(讲清理由)：cigar_butt/national_team/vi 取个股现价旁路未接 multi_source——multi_source 主源 TDX(TCP)不稳定时反而让这些功能变差，且腾讯/新浪直连已稳，接 multi_source 收益有限、风险(改长期稳定行为)>收益。

### 全项目审查优化收口
- 批次A(卫生)、批次B(安全加固)、批次C(回测bug修复+行情旁路DRY)全部完成。
- 仍待用户：回测真实数据重跑更新 CLAUDE.md 表。
