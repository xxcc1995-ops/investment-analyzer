# 2026-08-22

### 完成
- 改造个股 PE/PB/股息率历史走势图（`frontend/src/pages/StockAnalysis.tsx`，理杏仁式）：
  - 三张图各自独占全宽行，高度 350→420；图上方新增统计条（当前值/当前百分位/历史最高+日期/历史最低+日期/中位数）。
  - ECharts 配置：dataZoom（inside 滚轮缩放+拖拽平移 + 底部 slider 框选区间，filterMode:none）、tooltip 悬停显示该时点历史百分位（前端复刻后端 `_calc_valuation_stats` 口径：0<v<upperLimit(pe500/pb100/div30)，升序 <= 排名）、markPoint 标注历史最高/最低（红/绿圆点+值+日期）、yAxis scale:true。
  - ReactECharts 加 notMerge 防止切股时缩放状态残留。
- 构建验证：`npx vite build` TS 编译通过（4603 模块）。

### 决策
- 悬停百分位按**全历史**计算（与后端 stats.percentile 口径一致），而非随缩放窗口动态重算。
- 沙箱无法覆盖写入 `frontend/dist/index.html`（宿主提权后端占用/沙箱限制，`--emptyOutDir=false` 也 EPERM）→ dist 更新依赖用户重启 `启动.vbs`（先杀后端再自动 build，无锁问题）。
- agent-browser 未全局安装（装 Chromium 约500MB），放弃浏览器截图验证，以编译通过为准。

### 待续
- 用户需双击 `启动.vbs` 重启后生效（自动检测前端源码更新→自动 build→托管新 dist）。
