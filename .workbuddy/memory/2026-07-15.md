## 2026-07-15

### 完成 - 全项目审查（功能逻辑 + 软件工程）
- 用户要求检查整个项目各功能逻辑是否正确、有无优化点，优化前先读 CLAUDE.md。
- 已读 CLAUDE.md 对齐基准；梳理后端 ~40 路由/~40 服务、前端 ~34 页面。
- 精读地基代码：multi_source_quote（容错健全）、core/cache（LRU+TTL+交易时间感知，健全）、data_service（fetch_time+REPORT_DATE 排序，符合时效性）、quote_sources/base、cb_service（字段门控严格对齐「宁可空着」）、main.py（lifespan/统一异常/CORS/API_KEY 齐全）。
- 派 Explore 代理全量扫描工程坏味道。
- 产出报告：`docs/项目全面审查报告_2026-07-15.md`。
- 关键发现：
  - P0 无（无阻断性/数据造假缺陷）。
  - P1：15 处裸 except + 30 处 except-pass（交易类静默吞异常风险）；20 处服务绕过 quote_sources 聚合层各自抓行情（DRY+失容错）；大量路由缺输入校验；`/api/polymarket`、`/api/crypto-crawler` 未纳入 SENSITIVE_PREFIXES；CLAUDE.md 端口漂移（仍写 8002/5173，实际 8022/5180）；CLAUDE.md 回测表三策略指标完全相同(15.54%/-13.43%/0.93)待复核。
  - P2：死代码 fund_est_detail.py + 疑似孤儿 tushare_service/neodata；print() 散布；rate_limiter 同步阻塞；缓存装饰器固定 TTL 不动态。
- 结论：架构方向正确、关键风控到位，问题集中在工程卫生与一致性，非结构性缺陷。

### 决策
- 本次仅审查未改任何代码。报告按 P0/P1/P2 分优先级、分批次（A 卫生 / B 安全 / C 重构）建议，待用户选择从哪批开始。

### 待续
- 等用户选择优化批次或指定深入复核项（如回测三策略指标相同问题）。

### 完成 - 批次A（纯卫生，零功能风险）
用户选"从A开始"，已全部完成并验证：
1. **删死代码**：`api/fund_est_detail.py` 已删（确认仅 fund_arb.py 注释提及，无实际 import）。
2. **孤儿数据源服务**：`tushare_service.py`、`neodata.py` 全仓无引用，但 CLAUDE.md 列其为数据源对接层 → **保留+加状态注释**（标注"当前未被调用，保留作对接层，接入需配 TOKEN"），不盲目删。
3. **CLAUDE.md 端口同步**：启动命令/访问地址 8002/5173 → **8022/5180**，注明 8002 为僵尸端口勿用、--reload 去掉、补充 start.bat/start_backend.bat 用法说明。
4. **清 print()**：`prediction_market/polymarket.py`(4)、`opinion.py`(5) 加 logger 并 print→logger.error；`real_backtest.py`(2) print→logger.info；`backtest_service.py` __main__ 块 print→logger.info/error（修正了多参数 % 格式化 TypeError 隐患，改 f-string）。services/ 下已无 print 残留。
5. **敏感前缀补全**：后端 `security_middleware.SENSITIVE_PREFIXES` + 前端 `client.ts` 同步加入 `/api/polymarket`、`/api/crypto-crawler`，前后端一致。
- 验证：后端 `import app.main` = IMPORT OK；改动文件 `py_compile` 全 OK；前端 tsc 171 error 全为预存（client.ts 零错误，本次仅加字符串不可能引入类型错误）。

### 决策
- tushare_service/neodata 判断为"未接入数据源对接层"而非"死代码"，保留+注释是更稳妥的卫生处理（避免丢失 CLAUDE.md 登记的对接代码）。
- backtest_service __main__ 块保留（dev 测试入口），仅改输出方式。

### 待续
- 批次B（安全加固：输入校验补全 + 异常处理治理）与批次C（重构：行情旁路统一走聚合层 + 回测复核）待用户指示。

### 完成 - 批次B（安全加固）
1. **输入校验补全**：给 14 个接收代码参数的端点统一加 `validate_stock_code`（路径参数 + watchlist 的 code 查询参数）：
   - valuation.py 6 个（dashboard/percentiles/industry-comparison/alerts/chart-data/dcf）
   - right_side.py 4 个（analysis/backtest/signal-history + watchlist/add 的 code）
   - fund_arb.py 2 个（fund/{fund_code}、t-backtest/{fund_code}）
   - futures.py 1 个（history/{symbol}）、national_team.py 1 个（etf-flow/{etf_code}）
   - cb/jc/daily_info 无代码路径参数，跳过。
   - 自测：10 个合法代码（A股600519/港股00700/HK.00700/美股AAPL/基金161725/可转债113001 等）全部放行；5 个恶意输入（../etc、;rm -rf、超长、空格、DROP TABLE）全部 400 拒绝。
2. **裸 except 根治**：15 处 `except:` → `except Exception:`（opinion×3/futu_option/fund_service/tdx_source×2/macro/strategy_pairs_trading/strategy_mean_reversion/data_provider×5）。不再吞 KeyboardInterrupt/SystemExit。全仓已无裸 except。
3. **交易服务补日志**：grid_service.py、t_trading_service.py 原本**零日志**，新增 `import logging`+`logger`；8 处 `except Exception: pass` → `except Exception as e: logger.warning(...)`（grid 5：历史行情/股票数据/网格组合/健康历史获取失败；t_trading 3：做T服务/回测处理失败）。
- 验证：14 个改动文件 py_compile 全 OK；`import app.main`=IMPORT OK；校验函数自测通过；批次B 未改前端故无 tsc。

### 决策
- 校验插入位置统一在 docstring 之后、首条执行语句之前；锚点用各函数独有的 cache_key/try 行，保证唯一。
- `except:` → `except Exception:` 用 replace_all（`except:` 子串不会误伤 `except XxxError:`，因后者 except 后有空格）。
- grid/t_trading 的非 pass 类 except（continue/return/赋值）保留为 `except Exception:`（已是安全范围，仅缺日志），优先级低于 pass 类。

### 待续
- 其余文件的 `except Exception: pass`（valuation/macro/akshare/data_service/cross_analysis/fund_utils/fund_holdings/portfolio/right_side_service，这些已有 logger）约 20+ 处，为低风险机械后续，可另起一轮。
- 批次C（行情旁路统一走 multi_source_service + 回测三策略指标相同复核）待用户指示。

