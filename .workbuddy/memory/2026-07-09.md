## 2026-07-09

### 完成 - 侧边栏新增「套利」一级入口
- 改动文件：`frontend/src/components/AppSidebar.tsx`（纯前端，无后端依赖）
- 新增顶级分组 `套利`(key=`arb`，图标 `RetweetOutlined`)，内含两个子项：
  - 港股打新 → `/hki`（`HKIpoPage`，原在「另类投资」组）
  - 基金套利 → `/fund-arb`（`FundArbitragePage`，原在「基金与债券」组）
- 移除了这两项的原入口，**无重复**。`keyToPath`/`pathToKey` 映射原本已含这两个 key，无需改。
- 同步把 `arb` 加入 `rootSubmenuKeys` 与 `handleClick` 的忽略列表（父级点击不导航）。
- 图标 `RetweetOutlined` 已在 import 中补入。
- 验证：`npx tsc --noEmit`，AppSidebar 仅余 1 个**预先存在**的无关错误(`AlertOutlined` 未使用)，本次改动未引入新错误。

### 决策
- 用户要求"左侧一级入口增加套利入口，把港股打新、基金套利放进去"——按"移入"理解，从原分组删除避免重复入口（而非两处并存）。若用户想要两处都保留，需再确认。

### 待续
- 纯前端改动，前端 vite 热更新即生效，无需重启后端。用户刷新首页即可在左侧看到「套利」分组。
- 之前 8002 僵尸后端 / 启动器改造事项仍未最终验收（等用户双击新 `start.bat` 验证）；本次无关。
